language: cpp

compiler:
  - gcc
#  - clang

script:
  - make
  - make test

dist: precise
sudo: false

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
#      - llvm-toolchain-precise-3.6
    packages:
      - libboost-all-dev
      - g++-4.9
#      - clang-3.6
